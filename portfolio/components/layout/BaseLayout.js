import React, { Fragment } from 'react';
import Header from '../shared/Header';

const BaseLayout = () => {
  return (
    <Fragment>
      <Header />
    </Fragment>
  );
};

export default BaseLayout;
